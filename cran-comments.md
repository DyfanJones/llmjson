## R CMD check results

0 errors | 0 warnings | 0 notes

## Test environments

* local: macOS (darwin), R 4.4.0
* GitHub Actions:
  - macOS-latest (release)
  - windows-latest (release)
  - ubuntu-latest (devel, release, oldrel-1)

## Notes

This is a new submission.

This package wraps the Rust crate 'llm_json' for repairing malformed JSON
strings, particularly those generated by Large Language Models. It requires
the Rust toolchain (cargo >= 1.56.0) for installation from source.
